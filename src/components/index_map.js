import React from 'react';
import {connect} from 'react-redux';

function IndexMap(props){
  return(

  )
}

const componentCreator = connect(mapStateToProps);
export default componentCreator(IndexMap);
